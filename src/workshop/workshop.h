#pragma once
#include <string>

class WorkshopManager {
public:
    static std::string list();
    static std::string trending();
    static std::string uploadRoom();
    static std::string uploadInvention();
};
